# 架构师答复 #3／#5／#4 报告的「待架构师」+ 小修（2026-09-13 晚）[opus]

## A. 同修轮（`2026-09-13-fellow-practitioner.md` §3）

1. **自述没开始**：访客明说「没念过／还没开始／刚接触」→ 本通对话按入门轮答（三部起步），level 记 `beginner`；线索里的行话不能把一个自称没开始的人抬成同修。CC 封顶的做法保留，封到 `beginner`。
2. **义工设的 `contacts.stage`**：有值就**优先**（两个方向都以义工为准，可以降级）；没值才用线索／分类器取最高。
3. **R29 佛台结缘**：把《解答来信疑惑（第一百七十二篇）》「请下多尊菩萨像 → 21 张小房子、大悲咒心经礼佛各 7 遍」套到一位长者结缘佛台，是个案→通则。两处改：
   - 护栏：`letter_qa`（解答来信疑惑）的数字按**案例类**处理——只许转述口吻（「台长对某位同修开示…」），不许直接开成访客的功课／张数；通则性的上限句（「不能超过 108 遍」）转述即可，不受影响。加断言：R29 回复不得把 21 张／7 遍写成 📿 行或「你可以念…」。
   - 提示词（同修轮＋关怀）：佛台请下、结缘、搬迁、换像这类仪轨问题，只转述与访客情形相同的原文，情形不同就说明「这是另一种情况」，张数一律请义工面谈。
   - 委员会清单加一条「佛台结缘／请下佛台的如理如法做法」（架构师建智库草稿，待官方依据）。
4. **en／id**：overlay 模式下 zh 模块（含同修轮章节）本来就在 en/id 的系统提示词里；不译。第 4 天 watch 加一例 EN 同修问题（"I have practised for years, how do I raise my 境界?"）看效果即可。
5. R31 取第二通清修对话：可以。6. 分类器偏低一级：记着，不改。
7. **R28 第二轮**：两个都做——(a) 同修轮第一轮结尾不用是非问句（要问就问一个具体的、把话题往深处带的问题，或者不问）；(b) R28 第二轮只断言无尾巴／无 📿／无遍数／不问有没有念经，结构断言留第一轮。

## B. 小房子检索／祈求词（`2026-09-13-xfz-retrieval-and-prayer-guard.md`）

1. **探针每晚自动跑**：Vercel cron 满了就用 **GitHub Actions**——`.github/workflows/probe.yml`，每天 17:00 UTC（01:00 MYT，夜审 chips 刷新之后）`npx tsx scripts/probe-prod.ts`，失败即 workflow 红（Ken 收 GitHub 邮件）；不需要任何 secret（打的是公开 `/api/chat`，`browserId=test-suite:probe`）。写 watch 报告时引最近一次 run 的结果。
2. 「每晚自动跑与 R22 冲突」：R22（已在修梦到蛇）要求≥2 部经各带遍数，与「不替已在修的人定功课」冲突——**R22 改**：已在修的访客问梦境加什么 → 允许引原文里的经文（「凡是梦见蛇…要念消灾吉祥神咒、解结咒、心经和大悲咒」）并给标准范围（21、27 或 49 遍，来自卡）作为「一般」说法，不写「你每天 N 遍」的个人化功课；断言改为「点到原文里的经名 ≥2、遍数只以范围形式出现或不出现、无尾巴、无孤儿」。

## C. 第 3 天（`sonnet-watch-day3.md` §5）

1. 不回退——同意；09-14 00:30 那一跑才是第一次量 strip-tails 之后的 Sonnet 回复，NI > 26% 才动 `REPLY_MODEL_HIGH`。
2. **`citation_no_date` 口径改**：只在检索段落本身带日期而回复漏了时才标记；《解答来信疑惑》等来源没有日期的，引篇号即算完整，不标。31% 里大半是这类。
3. `quote_not_verbatim` 37 次（光明心／三界改写后进引文块）：同修轮「引文逐字」今天才上线，第 4 天看数；若仍高，下一步是**引文由程序从检索段落贴入**（模型只给段落编号），先记着。

## C2. 09-16 夜审的两条 NI（架构师 09-16 读库补充）

同一位访客两通（梦见前同事、问要不要念小房子、几张）：机器人给了经名和祈求词，张数说「那属于台长看图腾才能开示」「建议联系义工」。夜审判「该答不答」。判得对一半——**个人张数不替人定**没错，但《入门手册》的**一般说法**是官方通则，可以给：「一般初学者可以先念 4-10 张小房子给自己的要经者；梦见亡人一般至少 7 张，自己亲人 21 张及以上；确定不了数量就坚持给自己的要经者念」。架构师已把这段逐字加进 pinned 卡。提示词（小房子话题）加一句：**张数先给《入门手册》的一般说法，再说个人具体张数以师父开示或义工为准**——不要只转介。R32：09-16 那通原话（`353886cf`）→ 断言含「4-10 张」或「坚持给自己的要经者念」、无「只能台长看图腾」式拒答、仍有「以师父开示／义工为准」一句。

另两条 NI：「邪淫→皮肤病」无原文的因果推断（提示词已有「不在原文之外推因果」的规则，low 档波动，记着）；亡人超度一通前后矛盾（emotional_weight heavy）——已在复盘队列，义工跟进，不改代码。

## D. Ken 点「重同步」之后

Ken 在 `/dashboard/wisdom` 对卡 `15eb9282` 点「重同步」→ 18 条 chips 失效 → CC 跑 `warm-chips-prod.ts`，核对 zh `beginner_first_step` 三部、`family_conflict` 三部＋解结咒、祈求词长句、无占位符。

## 报告
写到 `docs/reviews/2026-09-13-architect-answers-2.md`；第 4 天 watch（09-14）另起 `docs/reviews/2026-09-14-sonnet-watch-day4.md`：夜审 Sonnet NI、quote_not_verbatim、citation_no_date（新口径）、citation_book_mismatch、needs_care／level 分布、探针 run、EN 同修一例。
