# 小房子检索根因 + 祈求词与护栏 + 本机 pinned 卡（2026-09-13）[opus]

这份补回 `2026-09-13-prayer-form.md` 丢失的 §4（架构师写在工作树、未提交，被分支操作丢掉；规则已加进 `AGENTS.md`），并回答 prayer-form 报告与 watch day-3 报告的「待架构师」。

## 1. 小房子经文组合检索不稳（prayer-form 缺失的 §4；day-3 §1.3 a/b）

生产 09-12 22:44：「一张小房子的经文组合是」→ 检索到《念诵指南》p38–39、p22–23、《例说》p35，**没有 84遍／87遍**，四个数被换成占位符；本地 R25 第 1 轮 5 次里 ≥3 次拿不到组合。写着组合的是《念诵指南》**第 2 页「2. 小房子的组成」**：「包括《大悲咒》27 遍、《心经》49 遍、《往生咒》84\n遍、《七佛灭罪真言》87 遍」。

1. **索引体检**：`audit-pinecone-books.ts` 查《念诵指南》第 2 页这一块在不在、文本是不是「84\n\n遍」断行；不在就补传（`content/books/`），在就重传这一块并先合行（`joinBrokenEnumerations` 只在护栏侧合行，不改索引文本）。
2. **小房子话题基线钉入**：整通对话里访客任何一轮出现「小房子」（或 敬赠／要经者／烧送／自存），`RetrievalContext` 标 `topic=xiaofangzi` 并**粘住整通对话**（day-3 §1.3 b：第 3 轮「还没」的检索只拼了上一轮，丢了小房子）；该话题每一轮钉入《念诵指南》第 2 页「小房子的组成」＋第 14 页「念诵者条件」两块（做法同 batch 1 失眠《入门手册》基线）。
3. **生产探针**：`scripts/probe-prod.ts`（`browserId=test-suite:probe`，is_test 契约）每晚夜审后打三问：「一张小房子的经文组合是什么」「小房子的篇数是多少？」「往生咒念多少遍」，断言四个数／标准数各自成对落地、无占位符、无尾巴；结果一行进当天 watch 报告。R17 一直只在本地绿、生产挂，就是缺这个。
4. **出处归属**（day-3 §1.3 c）：提示词加一条「引用某段原文时，书名以该检索段落的 metadata（book／post_title）为准，不要换成别的书」；护栏加软检查 `citation_book_mismatch`：引号内句子在检索段落中逐字命中时，句子前后 40 字内若出现《书名》且与命中段落的 book 不同 → 打标记（不 strip、不重试），进 review 记录；下一步再看要不要重试。

## 2. 祈求词与护栏（prayer-form 报告「待架构师」2–5）

1. **祈求词不进「> 」引文块**：提示词规定祈求词一律写成「念前祈请：「……」」的普通行（卡的写法），不用 Markdown 引文；引文块只放师父原文。
2. **护栏对祈求词行的处理**：以「祈请南无／请大慈大悲／念前祈请／祈求：」开头的行**豁免逐字引文核对**，改为只核对**开头**——开头必须逐字等于 pinned 卡的开头之一（「祈请南无大慈大悲救苦救难广大灵感观世音菩萨摩诃萨」或小房子／放生的「请大慈大悲观世音菩萨」「祈请南无大慈大悲观世音菩萨」），否则按原规则处理。个性化内容（名字、部位、对象）不再触发 `quote_not_verbatim`。加 N12：卡式祈求词带「（孩子名字）与（对方名字）」→ 放行；开头写成「祈请大慈大悲观音菩萨」→ 仍判。
3. **en／id 模块里嵌的中文祈求词**：同样换成卡的长句开头，英／印尼译文不变（译文本来只译内容）；`i18n` 表的祈求词断言按折叠开头比较。
4. **放生祈求词**：不改（《佛学问答》63／《吃素戒杀放生》的仪轨）。同意 CC 的判断。
5. **小房子祈求词按《念诵指南》**（不是现在 `xiaofangzi.ts` 的「慈悲我 XXX，把小房子送给」）：
   - 念诵前（p17）：「请大慈大悲观世音菩萨保佑我 XXX（自己的名字），帮助我将这些小房子送给 YYY」；化解冤结：「请大慈大悲观世音菩萨保佑我 XXX 与 YYY 化解恶缘」；自存：「请大慈大悲观世音菩萨见证我 XXX 所念小房子上的经文」。
   - 烧送前（p31）：「祈请南无大慈大悲观世音菩萨帮助我 XXX 能够将这些小房子送给 YYY」（有佛台先上香三称圣号；无佛台先上心香、一遍大悲咒一遍心经、举过头顶朝天三拜）。
   - 架构师已把这几句逐字加进 pinned 卡；`xiaofangzi.ts` 模板 F 与相关行改成这几句，追溯标 `ken-0913-xfz-prayer`；R24 的 `hasXfzPrayer()` 只认这两组。

## 3. 本机 pinned 卡（prayer-form §2 的根本解法）

本机没有 Supabase 凭据，回归跑不到生产的 pinned 卡（Pinecone 副本又是旧文本），所以 R25 才会看到组合缺失。架构师现在把已批准的 pinned 卡导出到 **`docs/canon/pinned-cards.json`**（每次改卡后重新导出，`updated_at` 与库一致）。

1. `getPinnedCanonPassages()`：无 Supabase 凭据时**读这个文件**（不再退回 Pinecone 的 `pinned` 过滤）；有凭据时仍读库。日志打一行来源（db／file）。
2. 回归脚本末尾照旧打印 `pinned cards attached: N (source=file)`。
3. Pinecone 副本：Ken 在 `/dashboard/wisdom` 退役再批准即可重同步（顺带 18 条 chips 失效 → 跑 `warm-chips-prod.ts`）；`wisdom-resync.ts` 留着备用。**智库路由加「已批准条目被编辑 → 重同步」**这次做掉：路由多一个 `resync` 动作（care admin），页面按钮「重同步」，对已批准条目重跑 `upsertWisdomRecord()` 并 `invalidateChipAnswers()`。

## 4. 回归与报告

- R17／R25 第 1 轮加断言「四个数成对落地、无占位符」；R24 用新的小房子祈求词断言；N12；探针三问。
- 全套一次（Sonnet 4.6 + v2 + 本机 pinned 卡 file 模式）。
- 报告 `docs/reviews/2026-09-13-xfz-retrieval-and-prayer-guard.md`，随代码提交合并。
