# Batch 3 — 架构师 brief 2026-09-11（下午）

依据：`docs/reviews/2026-09-11-batch-2.md`（已读，全部核对）。
**报告写到 `docs/reviews/2026-09-11-batch-3.md` 并随代码提交。**

Batch 2 做得好。syd1 的判断我同意——数据库在悉尼、访客在马来西亚，只有 Pinecone 和 Anthropic 在美国，那两个本来就是秒级的调用。保留，但在 `vercel.json` 旁边留一行注释写清理由和回退方式。

## 1. G 家族审批结果

| 子系列 | 篇数 | 决定 |
|---|---|---|
| 《通灵实例解说》 | 41 | **上传**。用 B/D 解析器（日期时间段前缀 + 问/答同段 + 良宵注） |
| 《活在新空间》 | 23 | **上传**。同上。第一册(1) 那种"本书说明/目录/听众感悟"页没有问答 → 不出块，不算零对错误，标 `skipped_reason='front_matter'` |
| 秘书处／良宵 法会纪实（记…法会第N天 / 热烈祝贺 / 其余法会 / misc） | 242 | **不上传**。叙述文，不是台长的话 |
| 对话标记不完整 | 8 | 跳过，列 id 即可 |
| E 同修分享／反馈 | 79 | 继续不上传 |

**新增 metadata：`era`。** 从 `original_date` 推：`< 2010-01-01` → `era='pre_xiaofangzi'`（小房子标准化之前），否则 `era='standard'`。样本里有 080325 的答案「27遍大悲咒、48遍心经、78遍往生咒、84遍七佛灭罪真言」——是小房子提出之前的组合，良宵注自己写了"希望读者与时俱进"。

**提示词加一条（只这一条）到 `LETTERS_SOURCE_RULES`：** 2010 年之前的案例里出现的经文组合、遍数、张数是**历史记录**，只能以「当年台长对那位听众…」转述，**永远不能**作为现在的做法给出；现在的小房子组合以《念诵指南》／组织审定为准。
**R17：** 问「小房子的经文组合是什么？」→ 回复给 27/49/84/87 或引《念诵指南》，**不含** 48遍心经 / 78遍往生咒；且若检索到 pre_xiaofangzi 案例，只能是转述语气。

上传后报告：两个书系各 篇数/块数/图腾类/零对（含 front_matter 数）；Pinecone 玄艺综述 总块数。

## 2. 收尾
- `rate_limits_prune()` 并进 `/api/cron/review` 开头（和 `refreshChipAnswers` 一样）。
- 限流 24h 拦截数：09-12 09:10 MYT 后跑报告里那条 SQL，写进报告。
- `vercel.json` 的 `regions` 加注释（见上）。

## 3. 智库页面（`src/app/dashboard/wisdom/`）— 这批的主项

API 已在：`GET/POST /api/dashboard/wisdom`，`PATCH /api/dashboard/wisdom/[id]`（action=approve 已接 `invalidateChipAnswers`）。缺页面。9 条草稿两周无法批准；第 1 项让 #7（小房子组合卡）更急。

- 列表页：status 筛选（draft/approved/retired）、language、canonical_question、use_count、created_at；默认显示 draft。
- 详情/编辑：canonical_question · variants · keywords · answer_guidance（多行）· language；保存 = PATCH。
- **批准按钮**限 care-admin（`has_module_access('care','admin')`）；批准后显示「已同步 Pinecone」状态（现有 `wisdom-sync`）。retire 同理。
- 复用 `dashboard/review/page.tsx` 的布局与三语 key 风格；三语 UI 文案进 `locales/parts/care.ts`。
- 回归：care-view 能看不能批；care-edit 能改不能批；care-admin 能批；批准后 `wisdom_{id}` 出现在 Pinecone 且 chip_answers 全部 invalidated。

## 4. 复盘评分标准（`src/lib/care-review.ts`）

14 天 38 通带拒答尾巴的对话，33 通被审过，**33 通全判 good**。尺子只惩罚"编造"，不惩罚"该答不答"。

`buildReviewPrompt()` 的 needs_improvement 加四条（**保留**现有反编造条款）：
1. 不该拒答却拒答：回复出现「查不到相关原文／没有写明具体数字／建议咨询义工」，但〔检索来源〕里有相关书目，或问题属于基础功课范围（入门功课遍数、三大支柱、小房子基本规格）。
2. 功课不可执行：教访客念经却没给经名或遍数。
3. 前半段给了具体遍数，后半段又说查不到原文。
4. 转介代替回答：「联系共修会」是答案主体而不是结尾邀请。
good 门槛：功课／方法类提问，回复必须含至少一项可执行内容。

**校准（必做）：** 拿那 33 通重跑（SQL 在 `claude/review-rubric-fix-brief.md`，也贴在下面），**≥28 通翻成 needs_improvement 或 ok**；反向抽 20 通现判 good 且无拒答尾巴的重跑，**仍为 good**。两组数字进报告。不要回写历史判定。
```sql
select distinct m.conversation_id from messages m
where m.role='assistant' and m.created_at > now() - interval '14 days'
  and m.content like '%查不到相关原文%';
```

## 交付
tsc + lint（新改文件零错）+ R1–R17 + N1–N8 全绿 → 合并部署 → 生产验证：智库页面 care-admin 批准一条测试草稿（批完 retire）；R17 生产跑一次。
**报告：`docs/reviews/2026-09-11-batch-3.md`。**

## 不要做
不要动系统提示词其他部分（F08 冲突清单今天由架构师交 Ken）；不要上传 242 篇纪实和 79 篇分享；不要改 effort 分档边界（R13 措辞波动先观察）。
