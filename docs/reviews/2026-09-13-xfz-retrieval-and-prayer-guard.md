# 小房子检索根因 + 祈求词与护栏 + 本机 pinned 卡 — 报告（2026-09-13）

简报：`docs/briefs/2026-09-13-xfz-retrieval-and-prayer-guard.md`（补回 prayer-form 丢失的 §4）· 分支 `feat/xfz-retrieval-prayer-guard` → main · 日志 `docs/reviews/xfz-retrieval-2026-09-13/`

## 0. 状态

| 项 | 状态 |
|---|---|
| 1.1 索引体检：《念诵指南》第 2 页「小房子的组成」 | ✅ 在索引里，但数字和「遍」被换行拆开 → 已合行重传（见 §1.1） |
| 1.2 小房子话题粘住整通对话＋钉入 p2／p14 两块 | ✅ |
| 1.3 生产探针 `scripts/probe-prod.ts` | ✅ 脚本；**每晚自动跑没做到**（见 §1.3、待架构师 1） |
| 1.4 出处归属：提示词一条＋`citation_book_mismatch` 软检查 | ✅ |
| 2.1 祈求词不进「> 」引文块 | ✅ 规则一条；关系类功课模板与两个示例去掉「> 」 |
| 2.2 护栏对祈求词行只核开头＋N12 | ✅ 单测 139/0 |
| 2.3 en／id 嵌入的中文祈求词换卡的长句开头 | ✅ 各 32 行 |
| 2.4 放生祈求词不改 | ✅ |
| 2.5 小房子祈求词按《念诵指南》p17／p31；R24 断言只认这两组 | ✅ |
| 3.1 无 Supabase 凭据时 pinned 卡读 `docs/canon/pinned-cards.json` | ✅ 日志打 `source=db|file` |
| 3.3 智库「重同步」动作＋按钮 | ✅（登录不了，页面请 Ken 看；**部署后请点一次卡 15eb9282 的「重同步」**） |
| 4 回归：R17／R25 第 1 轮四个数成对、R24、N12、全套一次、探针三问 | 全套（待补）；探针见 §5 |

## 1. 小房子经文组合检索

### 1.1 索引体检

用 Pinecone 直接查（`book=小房子念诵指南`）：

- **printed p2「小房子的组成」在索引里**：`xiaofangzi_guide_w2b2_14`（PDF 第 11 页；上传脚本记的是 PDF 页码，所以「第 2 页」查 `page_start≤3` 查不到）。按「一张小房子的经文组合是」不加过滤检索，它排第 1。
- **但文本是断行的**：`《大悲咒》 27\n\n遍 、 《心经》 49\n\n遍 、 《往生咒》 84  遍 、 《七佛灭罪真言》 87\n\n遍`。护栏侧的 `joinBrokenEnumerations` 会合行，模型读到的却是原文。`scripts/fix-xfz-p2-chunk.ts` 用同一个 id、同一份 metadata、合行后的文本重传了这一块，轮询确认 Pinecone 已返回新文本（`re-upserted ✓`）。
- **printed p14「念诵者条件」也在**：`xiaofangzi_guide_w2b2_20`（PDF 第 21–22 页），全段完整（先做功课才能念诵小房子…尚未开始念诵基本功课的初学者，需从基本功课《大悲咒》、《心经》、《礼佛大忏悔文》念起…当天可以念诵一些功课的《大悲咒》后即可开始念诵小房子）。`vector-search.ts` 里「p14 不在索引里（corpus gap）」的旧注释已经过时。
- **Pinecone 里卡的副本仍是旧文本**（1,714 字、没有小房子一节），生产读 Supabase 不受影响；本机回归之前读的是这份旧副本——这正是本地 R25 第 1 轮总拿不到组合的原因（见 §3）。

### 1.2 话题粘住＋钉入

- `retrievalContextFrom()`：之前任何一轮访客话里出现 小房子／敬赠／要经者／烧送／自存 → `ctx.topic='xiaofangzi'`；本轮话里有也算。
- 该话题下每一轮：检索 topics 补上 `little_house_baseline`（《念诵指南》基线查询照跑）；并按 id 取 `xiaofangzi_guide_w2b2_14`（p2 组成）＋`xiaofangzi_guide_w2b2_20`（p14 念诵者条件），跟 pinned 卡一样**附在结果后面、不参与排序、不被 topK 截掉**。两块进程内缓存 1 小时；只在中文界面钉（英文版是另一本书）。与检索查询并行取，不增加串行等待。

### 1.3 生产探针

`scripts/probe-prod.ts`：`browserId=test-suite:probe`（is_test，不进收件箱／计数／夜审），打生产 `/api/chat` 三问——「一张小房子的经文组合是什么」「小房子的篇数是多少？」「往生咒念多少遍」——断言 27／49／84／87 各自与自己的经名同行成对（枚举「21、27 或 49 遍」算成对）、无「（遍数以官方资料为准）」、无尾巴；每问一行，最后一行 `probe: N/3 ok`，失败 exit 1。

**「每晚夜审后自动跑」没有做到**：Vercel Hobby 的两个 cron 已用满（summarize、review），`cron/review` 里 chips 刷新就占了 270 s 预算（maxDuration 300），再塞三次生成会超时；仓库也没有 GitHub Actions。现在是 CC 写 watch 报告时手动跑、结果贴进报告。见待架构师 1。

### 1.4 出处归属

- 提示词（core.ts 引用规则）加一条：「书名以该检索段落的出处为准（【参考 N】那一行写的书名、篇名），不要换成别的书」。
- 护栏侧软检查 `citationBookMismatches()`：回复里每段引文（「> 」行，或 12 字以上的「…」／“…”）若逐字命中某个检索段落，前后 40 字内出现的《书名》（经名如《大悲咒》不算）必须对得上该段落的 book 或 post_title；对不上 → `flags` 加 `citation_book_mismatch`、写 `audit_log`（`care.citation_book_mismatch`），不删、不重试。祈求词行跳过。

## 2. 祈求词与护栏

### 2.1 不进引文块

- tiers.ts 入门轮第 7 条：祈求词写成普通行「念前祈请：「祈请南无…保佑我 XXX……」」，「> 」只放检索到的师父原文；小房子、放生照各自写法，同样普通行。
- `relationships.ts`：第 4 段功课模板、完整示例「我老公外遇，我想离婚」、「没念过」档示例共 55 行去掉「> 」，12 行「祈求："…"」改成「念前祈请：「…」」。追溯表把这一改写折叠回原 v1 行（`ken-0913-prayer-line`）。
- `practice.ts` 放生一节的「> 」祈求词没动（§2.4 不改）；护栏的开头清单包含放生写法，所以不会误判。
- 护栏 `isPrayerLine` 认「念前祈请」开头（孤儿祈求词判断）。

### 2.2 护栏：祈求词行只核开头

`verbatim-guard.ts` 引文核对：「> 」行若以 祈请南无／请大慈大悲／念前祈请／祈求： 开头（前面可有引号），去掉引导语后开头逐字等于以下之一 → 豁免逐字核对（其中数字同样按已核引文处理）：
`祈请南无大慈大悲救苦救难广大灵感观世音菩萨摩诃萨`（功课卡）、`祈请南无大慈大悲观世音菩萨`（《念诵指南》p31 烧送前）、`请大慈大悲观世音菩萨`（p17 念诵前）、`请大慈大悲的观世音菩萨`（放生，prompt 原文写法——简报没列，我加上以免放生引文块被误判，请确认）。开头不对的仍按原规则核对。

N12（`test-verbatim-guard.ts`）：卡式长句带「（孩子名字）与（对方名字）」→ 放行；「祈请大慈大悲观音菩萨」→ 仍判；p31、p17 开头 → 放行；普通师父原文仍逐字核对；普通散文不算祈求词行。**139/0**。

### 2.3 en／id

`en.ts`、`id.ts` 里嵌的中文祈求词开头换成卡的长句，各 32 行；放生与小房子的 5 行不动；英、印尼译文不变。i18n 核对按折叠开头比较，祈求词项不缺。**i18n 表里仍有缺项**：tiers 的礼佛祈求词（上午 xiaofangzi-entry 新增）、入门轮第 7 条、xiaofangzi 新的 p17／p31 祈求词、同修轮章节——这些中文新增都还没有英／印尼版（见待架构师 3）。

### 2.5 小房子祈求词（`ken-0913-xfz-prayer`）

与 pinned 卡逐字一致：
- 普通小房子步骤后新增「念诵前的祈请（p17）」：送给要经者／亡人／孩子／房子的要经者、化解冤结两句；「烧送前的祈请（p31）」：有佛台先上香三称、跪求「祈请南无大慈大悲观世音菩萨帮助我 XXX 能够将这些小房子送给 YYY」，无佛台上心香、一遍大悲咒一遍心经、举过头顶朝天三拜。
- 自存：两句「请大慈大悲观世音菩萨作见证，我XXX…（保佑我身体健康）」→ p17「请大慈大悲观世音菩萨见证我 XXX 所念小房子上的经文（…）」；保留「不加任何自己的祈求」。SOP 第 10 条同步改。**「确定将来给自己就加『保佑我身体健康』」这一变体来自《佛学问答》55，《念诵指南》没有，被去掉了**（待架构师 2）。
- 模板 F「烧送前,在菩萨前祈求」→ p31 原句。
- R24 的 `hasXfzPrayer()` 只认 p17「请大慈大悲观世音菩萨保佑我／见证我」与 p31「祈请南无大慈大悲观世音菩萨帮助我」。

追溯：v2 中文非空行 1,984，追溯 1,886，新增 98，**追溯不到 0**；v1 未进 v2 的 91 行全部有依据（本简报删 8 行：自存两种情况 6 行、SOP 第 10 条、模板 F 一行）；卡格式核对范围内短句 0。

## 3. 本机 pinned 卡

`getPinnedCanonPassages()`：有 Supabase 凭据读库，没有就读 `docs/canon/pinned-cards.json`（`buildWisdomRecord()` 同一套拼法，与生产逐字相同），不再退回 Pinecone 的 `pinned` 副本。每次刷新打一行 `[vector-search] pinned cards source=db|file n=…`；回归脚本最后打印 `pinned cards attached: N (source=…)`。

智库路由加 `resync`（care 管理员，只对已批准条目）：用当前行重跑 `upsertWisdomRecord()`，再 `invalidateChipAnswers()`（chips 与 pinned 缓存一起失效），写 `wisdom_resynced` 审计；页面在「退役」旁加「重同步」按钮（中／英／印尼文案）。

## 4. 回归

全套一次（`suite-full.log`，Sonnet 4.6 + v2，`--concurrency 6`，**pinned 卡 source=file**：`wisdom_15eb9282…`，$6.15）：**195/201 checks，35 例 30 例全绿**。

本简报直接相关的全部通过：
- **R17**：新断言「27／49／84／87 各自成对、无占位符」✓，回复引了《念诵指南》p2 原文（合行后的文本）。
- **R25**：`[turn 1]` 四个数成对 ✓（上午 5 次里 ≥3 次拿不到）；最后一轮三部＋门槛句照旧 ✓。
- **R24**：p17「念前祈请：「请大慈大悲观世音菩萨保佑我 XXX…送给 XXX 化解三六九关劫」」＋p31「烧送前跪求：「祈请南无大慈大悲观世音菩萨帮助我 XXX…」」，都是普通行 ✓。
- 各例回复里的功课祈求词都写成「念前祈请：「…」」普通行。

挂的 5 例：

| 例 | 挂的 | 判断 |
|---|---|---|
| R6 玄艺问答 | 引了但没带节目日期 | 既有的措辞波动（batch 4、prayer-form 全套都挂过） |
| R11 时差要不要加倍 | 「回复里的遍数都要在检索段落里」 | **断言的假阴性**：回复写「礼佛…每天 1-3 遍左右（初学者从 1 遍起）」，卡里是「每天 1-3 遍」；断言用裸 `\d+遍` 抽数，「1-3 遍」只抽出 3遍、没有 1遍。护栏自己的抽取器认范围，这条回复 0 残留。已改成范围、枚举里的每个数都算，复跑见下。本地现在读到的是生产同一张卡（数字更全），所以这条之前没暴露 |
| R22 已在修梦见蛇 → 大悲咒21 → 心经21 | 「至少两部经各带遍数」 | 最后一轮在问「礼佛有念吗？往生咒呢？」，没开功课。卡的用法一句写着「**已在修的人不替他另定功课**」，同修轮简报也是这个方向；R22（09-12 strip-tails）要求给两部带数——**两份要求冲突**，见待架构师 6。09-12 pass B 也挂过（只给一部） |
| R28 同修轮 提高境界 → 有 | 第二轮 ≥2 段引文／出处 | 同修轮报告待架构师 7 |
| R29 安老院 | 正文 > 300 字 | 修正后 3 次里挂 1 次（波动） |

R11 复跑（`suite-R11-rerun.log`，断言改后）：**4/4**，护栏 clean，pinned 卡 source=file。

## 5. 部署与生产探针

**部署前基线**（`probe-prod-before-deploy.log`，07:31 UTC）：组合两问 ✓（生产读库里的卡，已有组成一节）；「往生咒念多少遍」回复是正确的「《往生净土神咒》（往生咒）一般需要念诵 **21、27 或 49 遍**」，但探针第一版只认「21遍」连写、把枚举判成缺——**探针的问题**，已改成枚举也算成对。同一条回复把祈求词放在「> 「…」」引文块里——正是 §2.1 要改的，部署后复查。

**部署**：合并 `3a4d782` 推到 main，**07:40 UTC（15:40 MYT）** 生产 `/dashboard/wisdom` 的 JS chunk 出现 `wisdom.resynced`——新版本在线。

**部署后探针**（`probe-prod-after-deploy.log`，07:40 UTC）：**3/3** ✓——组合两问四个数各自成对、往生咒 21（在「21、27 或 49 遍」里）成对，无占位符、无尾巴。

**请 Ken 做一下**：`/dashboard/wisdom` 打开卡 `15eb9282`，点新的「**重同步**」——Pinecone 里那份卡的副本才会带上小房子一节（生产检索读库不受影响，但 Pinecone 副本是旧的）；这一步会让 18 条 chips 失效，之后我跑 `warm-chips-prod.ts`（`beginner_first_step`／`family_conflict` 的缓存还是三部口径之前生成的，正好一起换掉）。

## 6. 待架构师

1. **探针每晚跑的方式**：(a) 仓库加一个 GitHub Actions 定时工作流跑 `probe-prod.ts`（每晚三次生成，失败 GitHub 发邮件）；(b) 升 Vercel Pro 多一个 cron；(c) 维持 CC 写 watch 报告时手动跑。
2. **自存小房子「保佑我身体健康」变体**（《佛学问答》55）按简报改成《念诵指南》p17 后被去掉；要保留就加回一句「确定将来给自己的要经者，可按《佛学问答》55 加『保佑我身体健康』」。
3. **en／id 译文落后**：礼佛祈求词、入门轮第 7 条、p17／p31 小房子祈求词、同修轮章节都只有中文。
4. **索引里还有别的断行数字**：只修了 p2 这一块；例如同书 `_43`（p52–54）也有「87\n\n遍七佛灭罪真言」。全书合行重传是另一件事，要做请说。
5. 护栏开头清单多加了放生的「请大慈大悲的观世音菩萨」（§2.2），请确认。
6. **R22 与「不替已在修的人规划功课」冲突**（§4）：访客说了自己在念大悲咒 21、心经 21，卡的用法和同修轮方向是不替他另定功课；R22 断言要「至少两部经各带遍数」。二选一：R22 改成「不另定功课、可以建议加念对应小咒（带不带数都行）」，或卡的那句对「问梦、问要加什么」的访客放宽。
