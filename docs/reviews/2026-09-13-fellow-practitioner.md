# 同修轮：老同修不再被当初学者 — 报告（2026-09-13）

简报：`docs/briefs/2026-09-13-fellow-practitioner-mode.md` ＋ 增补 `…-addendum.md`（方案 1：关键词线索，回复前、零延迟）· migration 051 架构师已 apply · 分支 `feat/fellow-practitioner` → main · 日志 `docs/reviews/fellow-practitioner-2026-09-13/`

## 0. 状态

| 项 | 状态 |
|---|---|
| 判级 `level`：回复前线索（本轮＋本通对话所有访客轮）× 已持久化 `conversations.level` × `contacts.stage`，取最高 | ✅ |
| 回复后 Haiku 分类器多返回 `level`／`needs_care`，写回 `conversations`，只升不降 | ✅（提示词调过一轮，见 §2.2） |
| tiers.ts【同修轮】章节＋长度表一行＋提示词兜底；practising 行；长者关怀 | ✅ 追溯 `ken-0913-fellow` 15 行，追溯不到 0 |
| 检索：experienced 不钉《入门手册》功课基线；《白话佛法》《法会弟子提问》《解答来信疑惑》加权，玄艺个案排后 | ✅ |
| effort：experienced 至少 medium | ✅ |
| 护栏：experienced 不加任何尾巴；重试提示改为「去掉数字」 | ✅ |
| `needs_care`：线索＋分类器；收件箱「关怀跟进」筛选 | ✅ |
| R26–R31 ＋ 反例 F1／F2，Sonnet 4.6 两遍 | pass A 107/109、pass B 106/109；修正后复跑见 §2.1 |
| chips 不受影响 | ✅ 六条入门题判级都是 new（单测） |

## 1. 怎么做的

### 1.1 `src/lib/prompt/level-cues.ts`（纯函数，回复前）

- 线索表照简报 §一：experienced（清修、境界、开悟、光明心、三界／六道、弘法／度人、弟子／拜师／莲花、许过的愿、自存、要经者、7749、369／本命年小房子、佛台结缘／请下／换像、「修了几年」「我每天大悲咒 49」、引师父／台长的话发问、年事已高／安老院）；practising（在念功课／小房子、有佛台、问遍数张数、问小房子）；beginner（刚开始、不会念、跟视频、念了几天）；`needs_care`（年事已高、安老院、身体不好、孤单、独居、家人不修…）。
- **扫本通对话所有访客轮，取最高**（增补第 1 条）。
- **一处偏离「取最高」，请架构师确认**：访客**自己说**还没开始（「还没」「没念过」「刚开始」「不会念」）时，只由话题推出来的 practising（例如问到「小房子」「几遍」）降为 beginner。不这样做，R13「我想开始念小房子 → 还没有开始念功课」和 R25（18f74524）会因为「小房子」判成 practising，丢掉上一份简报刚落实的三部入门轮。experienced 信号从不被降。
- `contacts.stage`：`steady_practice`（同修）／`volunteer`（义工）及旧值「同修／义工／共修者」→ experienced；`started_chanting`（新同修）／「学习中」→ practising；`attended`（佛友）→ beginner；新佛友不设级。简报正文写「stage 存在时优先」、增补写「取最高」——按后写的增补做了**取最高**（义工设了「新佛友」不会把线索判出的 experienced 拉低）。
- 单测 `scripts/test-level-cues.ts` **34/0**：R26–R31 六例回复前即 experienced；简报「为什么」里其余老同修原话（三界幻相、莲花两次机会、烧送结缘小房子、369、菩萨像换瓷像、7749）；F1「工作一直不顺，是不是有业障？」→ new；F2「小房子的经文组合是什么？」→ practising；R12／R13／R14／R25 与四条 chip 式入门问题 → new／beginner；「帮我超度一位亡人」不算度人；stage 与 maxLevel。

### 1.2 管线

- **网页**（`/api/chat`）：`loadPersistedLevel`（`conversations.level／needs_care` ＋ contact 的 `stage`；首轮按 browserId 读 stage）并进检索前原有的 `Promise.all`（AI 草稿开关、chip 缓存），不新增串行等待；`decideTurnLevel` → `ctx.level`。每轮一行日志 `[level] … level= source=cues|persisted|stage needs_care= cues=[…]`。
- **本轮判级块**：experienced／practising／需要关怀时，在检索上下文（不缓存的那一块）末尾加一小段指令；new／beginner 不加——系统提示词缓存不受影响，入门轮行为与之前逐字相同。
- **WhatsApp** 与 **失败回复重试**（`reply-recovery` 走 `generateReply`）用同一套判级。
- **回复后**：`classifyAndSaveCategory` → `levelUpdateFor`：读当前值，`max(当前, 线索, 分类器)`，`needs_care` 只开不关，只写有变化的列；分类器失败时线索判出的级别照样写。`contacts.stage` 不写（读时覆盖，按 051 注释）。

### 1.3 检索、effort、护栏

- `level=experienced`：去掉 `homework_baseline`（不钉《入门手册》功课块）；书名以《白话佛法》《法会弟子提问》《解答来信疑惑》开头的段落 +0.04，`case_qa`（玄艺问答／综述个案）−0.02；小房子与教义数字的基线照旧。
- effort：experienced 时 low → medium；教义数字仍 high。
- 护栏：`chooseGuardTail(…, { level })` 对 experienced 返回 `none`；重试指令换成「同修轮不写遍数：把这些数字连同所在句子改成不带数字的建议，不要换别的数字，不要写查不到」。引文逐字规则不变。

### 1.4 提示词（tiers.ts，`ken-0913-fellow`）

【同修轮】章节放在入门轮规则之后、Tier 0 之前：增补第 3 条兜底句（系统判 new／beginner 但话里显示已在修，照样按同修轮）；不出 📿、不写遍数（明确问才按卡／161）；不问有没有念经；语气；五步固定结构（共情 → 师父原文 2–3 段、「> 」逐字、书名以检索段落为准 → 白话讲 → 用话说的建议＋「多读《白话佛法》」＋ https://xlfm.my/read → 圣号或鼓励、无尾巴）；practising 行；长者／需要关怀。长度表加「同修轮 ≤ 600 字（引文不计）；长者关怀 ≤ 300 字」。复跑后补了一行「访客只简短回答你上一轮的问题时，直接接着讲，不再反问」（R28，见 §2.3）。

追溯：v2 中文非空行 1,998，追溯 1,909，新增 89（本简报 15），**追溯不到 0**；数字／链接／电话／祈求词清单不变。

### 1.5 收件箱

`/api/dashboard/conversations` 列表项带 `needsCare`、`level`；`counts.needsCare` 为被标记对话数（一次查 id，≤1,000 时精确）；被标记但不在最新 1,000 行里的对话照「未回复」的做法补进列表。页面多一个「关怀跟进」标签（琥珀色角标）与空状态文案（中／英／印尼）。**我登录不了收件箱，页面效果请 Ken 看一眼。**

tsc、eslint 通过；护栏单测 133/0。

## 2. 验证

### 2.1 回归（Sonnet 4.6 + v2，本地管线；每例都走与生产相同的回复前判级）

`--only R26,R27,R28,R29,R30,R31,F1,F2,R12,R13,R14,R18,R25,R4,R24`（R4「莲花海水池」、R24「369 小房子」会被判成 experienced，一起跑看有没有伤到）：

| 轮次 | 结果 | 挂的 |
|---|---|---|
| pass A（`suite-passA.log`，$5.96） | **107/109**，15 例 14 全绿 | R28 第二轮：无引文块、无《书名》 |
| pass B（`suite-passB.log`，$1.52） | **106/109**，13 全绿 | R28 同上；R29 正文 > 300 字 |
| 修正后 R28／R29 ×2、R26 ×1（`suite-fix-{C,D}.log`） | C 21/22、D 12/14 | **R29 两遍都 ≤ 300 字（已修）**；R26 绿；R28 C 第二轮接着讲、引了《佛言佛语（十）》一段（只差「≥2 段」），D 仍反问「是哪里觉得卡住了」 |
| R28 ×2，日志补印前一轮结尾（`suite-r28-{E,F}.log`） | 6/8、6/8 | 第二轮：E 接着讲、引一段法会原文但只写「师父在法会里说过」（缺《书名》、只有 1 段）；F 第一轮结尾问「你现在修行上遇到什么让你觉得卡住的地方吗？」，访客答「有」，第二轮问「可以说说是什么地方让你感觉卡住了吗？」 |

**R28 合计 6 次第二轮**：无尾巴 6/6、无 📿 6/6、无遍数 6/6、不问有没有念经 6/6；「≥2 段引文＋《书名》」0/6（C 最接近：一段、有书名）。**F 那次说明了原因**：第一轮自己问了一个开放问题，访客只答「有」而没说是什么，这时追问一句是对话上正确的做法——断言要求它照样贴两段原文，反而不自然。见「待架构师」7。

两遍里全绿的：R26、R27、R30、R31（无 📿、无遍数、≥2 段逐字引文、不问有没有念经、有出处、无尾巴），F1、F2（F2 两遍都给齐 27／49／84／87、无占位符），入门例 R12／R13／R14／R18／R25，R4、R24。

### 2.2 分类器（回复后那一层）

同一批访客原话单独喂给 `classifyConversation`：

| 版本 | 结果 |
|---|---|
| 第一版（英文等级定义） | 格式解析正常；但 R27 光明心 → new、R28 → practising、R30 度女佛友 → new、F2 → new（偏低；因为只升不降，线索已经判对，不会造成错误写入，但这一层就没起作用） |
| 加中文信号清单＋「拿不准一律取高」 | 六例全 experienced，R29 needs_care=1；**副作用**：小房子类问题的类别跑成「学业」 |
| 加「念经／功课／小房子／佛台／许愿／放生 → 修行方法，学业只指上学考试」 | 六例 experienced、类别全对；F1 new／事业财运；「孩子明天考试…」new／学业；R13 beginner；F2 → beginner（线索是 practising，取最高后仍是 practising，无害） |

### 2.3 两个复跑发现的问题与处理

- **R28「要怎样才能提高境界 → 有」**：第一轮很好（《白话佛法（第二册）》原文），第一轮结尾问了访客一句，访客答「有」；第二轮两遍都**反问**「你说的有是指什么」，没有原文、没有出处。提示词加一行「访客只简短回答你上一轮的问题时，直接接着讲，不再反问；这一轮同样按固定结构」，本轮判级块同样补一句。
- **R29 安老院佛台结缘**：pass B 正文超过 300 字（还附了共修会电话与网址）。「关怀」判级块写明「正文不超过 300 字（引文不计）」。

## 3. 待架构师

1. **自述没开始封顶 practising**（§1.1）：与「取最高」的字面有出入，理由是保住上一份简报的三部入门轮。
2. **stage 取最高还是优先**：简报正文与增补写法不同，按增补做了取最高。
3. **R29 回复的内容**：两遍都引《解答来信疑惑（第一百七十二篇）》「大家一起帮忙念诵21张小房子…大悲咒、心经、礼佛大忏悔文各7遍」——那一篇讲的是**请下多尊菩萨像**，模型把它套到这位长者一张佛台结缘出去；护栏放行（数字逐字出自检索段落，`letter_qa` 不算个案类型）。这算不算个案→通则，要不要有一条组织审定的「佛台结缘／请下」答案，请定。
4. **en／id**：【同修轮】章节只在中文模块；本轮判级块是中文指令，en／id 的 experienced 访客能看到指令但系统提示词里没有章节。要译的话同 batch 4 的做法。
5. **R31**：简报正文列 R26–R30，增补与队列写 R26–R31 六例；第六例我取了生产的第二通清修对话 `6a6134b1`「清修遇到缘分怎么办 要怎样化解坚定清修路」。两通「光明心」只用了第一通。
6. 分类器 F2 仍偏低一级（beginner vs practising）；只升不降下无害，记着。
7. **R28 第二轮的断言**（§2.1）：简报写「同上；第二轮无尾巴」。无尾巴 6/6 做到了；「≥2 段引文」0/6。第一轮结尾若是开放问题（「有没有卡住的地方？」），访客答「有」之后追问是合理的。二选一：(a) 同修轮第一轮不以是非问句结尾（提示词再加一条）；(b) R28 第二轮只断言无尾巴／无 📿／无遍数／不问有没有念经，结构断言留给第一轮。我没有自己放宽断言，R28 目前按原断言记为红。

## 4. 部署

合并 `9227dec` 推到 main，Vercel 自动部署；**07:16 UTC（15:16 MYT）** 生产 `/dashboard` 的 JS chunk 出现 `care.filterCare`——新版本在线。

**生产探针**（`browserId=test-suite:probe-fellow-0913`，is_test）「如何修光明心」：无 📿、引文外无遍数、不问有没有念经、无尾巴 ✓；「> 」引文块 **1** 段（同修轮要 2–3 段），结尾问了一句「你现在在哪个方面觉得心比较暗、比较难放下？」——与本地 R27 两遍全绿相比，生产这一次引文少一段，记进观察。收件箱「关怀跟进」标签请 Ken 登录后看一眼。
