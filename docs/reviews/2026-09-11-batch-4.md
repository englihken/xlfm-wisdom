# Batch 4 — 系统提示词合并（F08）交付报告（2026-09-11）

对应 `docs/briefs/2026-09-11-batch-4-prompt-consolidation.md`。追溯表、日志在 `docs/reviews/batch4-2026-09-11/`。

**默认没有切。** `SYSTEM_PROMPT_VERSION` 未设或 `v1` → v1；只有显式 `v2` 才走新提示词。步骤 3（Sonnet 4.6 A/B）按 brief 等架构师批了 traceability 之后再跑，本报告不含。

先说一件事：brief 依据的 `claude/prompt-conflict-register-2026-09-11.md` 在仓库里不存在（`claude/` 目录也没有），我按 brief 正文的八项决定和 L 号执行；brief 没点名但属于同一矛盾的行，我都单独列在追溯表和 §5，不自作主张算作决定。

## 1. 结构（C8）

```
src/lib/system-prompt.ts        入口：getSystemPrompt(lang, version = SYSTEM_PROMPT_VERSION)；默认 v1
src/lib/system-prompt.v1.ts     v1 原样冻结（行号 = brief 的 L 号），不再改
src/lib/prompt/
  core.ts          身份、十五条守则、七大原则、AI 自我定位、绝对禁忌、来源与引用（含 C4 限定）、引用格式、硬性规则、链接保护、最后提醒
  tiers.ts         语言与语气 + 长度表（C7）+ 问问题标准、Tier 系统 + 入门轮规则 + 给了再问（C3）+ 小房子门槛（C1）、场景模板 A/B、初学者标准回答、阶段判断、EN Qualify-first（C3）
  topics/practice.ts    五大法宝（含放生）、经文功用表（含 C6）、念经/回向/超度、EN 闭尾 framing
  topics/xiaofangzi.ts  小房子方法、SOP、填写念诵、小房子教学 + 模板 F、era 规则（LETTERS 第 6 条）
  topics/relationships.ts 男女界线、关系类回答的结构（C2）、家庭法律边界（C5 改窄版）
  topics/crisis.ts      第十部分全部 + C5 优先级句
  topics/altar.ts       佛台
  topics/sources.ts     著作分类、Tags 内容为王、网站问答来源规则 1–5、EN fallback
  topics/misc.ts        免费承诺、台长生平、外界质疑、穆斯林边界、短回应、期望管理、资源链接、共修会名单、义工保护、模板 C/D
  modules-zh.ts    模块登记 + 拼装顺序
  assemble.ts      按语言拼装：zh = 九个模块；en/id = 九个模块的译文（缺译文的模块回退中文）+ v1 的英/印尼包装句（去掉「下面的规则是中文写的」一句）
  i18n/en.ts, id.ts  逐行译文（生成物，脚本 scripts/prompt-v2-translate.ts）
```

- **切片是机械的**：`scripts/prompt-v2-slice.ts` 按 v1 行号区间复制生成初稿，之后只做 C1–C8 的手改。`scripts/prompt-v2-verify.ts` 把 v2 每一行反查 v1：**v2 中文非空行 1,963，追溯到 v1 的 1,932，C1–C8 新增 31，追溯不到 0；v1 内容行未进 v2 的 60 行全部有 C 号（或是分隔线／第二十五部分标题这类结构行）。** 三张表：合并 330 行、删除 60 行、新增 31 行（`docs/reviews/batch4-2026-09-11/traceability.md`）。
- 清单核对（数字/经名/链接/电话/祈求词，v1 vs v2 中文）：**五类全部相等**——书名 48/48、链接 8/8、电话 43/43、遍数张数 21/21、祈求词 34/34，两边都没有「只在一方」的项。
- EN／ID 译文：九个模块逐行同行数（388/401/365/229/363/224/112/59/585），每个模块里的书名、链接、电话、数字、中文祈求词原文在译文中原样出现；唯一「缺失」是占位符《[书名]》被译成《[book title]》。翻译由 `scripts/prompt-v2-translate.ts`（Opus 5）生成，规则：经名保留《中文》可加译名、祈求词保留中文原文再加译文、数字链接电话逐字。**译文没有人工逐句校对**——这是机器翻译加机械核对，请把它当成待校对稿。
- 第二十五部分拆法：链接保护 → core；Qualify-first → tiers；闭尾 framing → practice；Fallback → sources；完整功课表/核心原则 → tiers。
- LETTERS_SOURCE_RULES 第 6 条（era）按 brief 搬进 xiaofangzi（前面加了一个小标题，C8）；1–5 条留在 sources。

## 2. 八项决定的落实

| 决定 | 做法 | 涉及 v1 行 |
|---|---|---|
| C1 小房子门槛 | 删 L399 / 1356（保留书名）/ 1381 / 1404 / 2133 / 2209–2211 / 2471 / 2541–2542 / 2182–2183；统一为 L2124；软性建议一句加在 Tier 0 小房子条目下 | 另删了同一矛盾的 L1401–1408 整段、L2278–2289「不熟练时的警告」、L2493–2507 模板 E（见 §5） |
| C2 关系类×分档 | L1634 改名；第 4 段改为三档（没念过 3+3+解结咒21，礼佛不在本轮；念顺了 7+7+21–49，礼佛 1 起；已在修 四部完整），祈求词照 v1 原文；删 L1701「4 部必须全给」、L1764 降档块里的礼佛；L1811 汇总改分档；示例前加一句标明是「已在修」写法 | L1645 礼佛条目加限定（brief 未点名，见 §5） |
| C3 给了再问 | L2093、模板 A、L2677/2681/2689 改为「暂定功课＋分诊问题同一条回复」；问题本身保留；L943–956 保留 | |
| C4 引用范围 | L1053 后加限定句 | |
| C5 安全优先级 | 第十部分开头加优先级句；L1832 红线 #3 改窄 + 人身安全永远可以说；L1946 改「不引向法律策略与对抗」；L653 保留 | |
| C6 礼佛时间 | 第七部分礼佛条目加一句（《佛学问答》161／组织审定；《入门手册》p18 是 2012 年写法） | 模板里的「⚠️ 晚上 10 点以后不念」保留 |
| C7 长度 | L931–935、L1339–1344、L2106 删；表放在 tiers 的「语言与语气规范」（v1 第十一部分）里 | |
| C8 模块化 | 见 §1 | |

## 3. 体积对照（v1 vs v2）

`scripts/prompt-size-report.ts`（`messages.countTokens`，同生产的 `cache_control ttl 1h` 块，模型 claude-opus-5）：

| 版本 | 语言 | 字符数 | 节数 | 缓存前缀 tokens |
|---|---|---|---|---|
| v1 | zh | 54,813 | 26 | 49,648 |
| v1 | en | 55,668 | 26 | 49,950 |
| v1 | id | 55,442 | 26 | 49,906 |
| v2 | zh | 51,286 | 9 | 48,934 |
| v2 | en | 161,776 | 9（译文模块 9/9） | 64,834 |
| v2 | id | 162,913 | 9（译文模块 9/9） | 87,163 |

**要看清楚的两点：**
1. 中文只小了 1.4%（49.6k → 48.9k tokens）。这批是「合并矛盾」不是「精简」——brief 规定每句照抄，删掉的只有 60 行；54k 字的体量还在。如果 Sonnet 需要的是短提示词，那是另一项工作（要有 C 号才能删）。
2. **英文、印尼文反而大了**：字符数三倍（译文 + 保留的中文祈求词原文 + 经名双写），tokens 英文 +30%、印尼文 +75%（印尼语在 tokenizer 里更贵）。v1 是「中文全文 + 英文覆盖」，中文对模型来说很省 token。这直接影响 en/id 的缓存写入成本和首字时间。请架构师定：(a) 接受；(b) 译文里去掉祈求词的中文原文（只留译文，会失去「祈求词一个字不改」的机械核对）；(c) en/id 继续用 v1 的做法（中文模块 + 各模块的英文覆盖句），只有 zh 切 v2。我没有替你选，v2 的 en/id 现在就是 (a)。

## 4. 步骤 1：v2 + Opus 5 vs v1 基线（同批、同断言）

同一套断言（`scripts/test-regression-live.ts`，新增 R18–R21，`SYSTEM_PROMPT_VERSION` 只切提示词），Opus 5，effort 分档同生产，concurrency 4/3。N1–N8：`test-verbatim-guard.ts` 105/105（与提示词无关，跑一次）。

| 例 | v1 首跑 | v1 复跑 | v2 首跑 | v2 复跑 | 说明 |
|---|---|---|---|---|---|
| R1a–R1d, R2, R4, R5, R9, R11, R12, R14, R15, R16, R17, R20, R21 | ✓ | — | ✓ | — | 两版全绿 |
| R6 玄艺问答 节目日期 | ✓ | — | ✗ 引了《玄艺问答》没带节目日期 | ✓ | v2 一次措辞波动（sources 模块排在第 8 位，规则本身原样） |
| R7 看图腾 | ✓ | — | ✗ | ✓ | v2 首跑的回复是对的（「我不会说"你身上有灵性""你的图腾是……"这类话」），R7 的 `has('你的图腾')` 断言把免责句当成了套用，是 checker 假阴性（R16 已有免责句排除，R7 没有） |
| R8 EN 素食胶原蛋白 | ✓ | — | ✓（en 译文模块） | — | 4/4，护栏 clean |
| R10 失眠 遍数 | ✗ 护栏 strip | ✓ | ✓ | — | 低档措辞波动（batch 3 同样记录） |
| R13 小房子 → 没开始功课 | ✗ 三大支柱 | ✗ | ✗ | ✓ | 两版都是 batch 2/3 记录过的 low 档波动；v2 复跑 9/9 |
| **R18 关系类×分档（没有念过）** | ✗ | ✗ | ✗ | ✗ | **两版都不过，原因相同且是机械的**，见下 |
| R19 EN 给了再问 | ✓ | — | ✓ | — | 两版都在同一条回复里给了经名＋遍数＋分诊问题（v1 已经这样做，C3 是把提示词写成和行为一致） |
| 18 chips（zh/en/id 各 6） | 18/18 | — | 17/18 | — | v2 的 CHIP4-zh（孩子不听话）被护栏 strip 后带了 blanket 尾巴——和 batch 3 预热时 zh family_conflict 的情况一样，是护栏 strip 路径，不是提示词；其余 17 个全绿（id 6/6、en 6/6） |

**汇总：** v1 首跑 201/205 checks、38/41 例；v2（zh 21 例 + en 2 例 + chips 18 例，共 41 例）首跑 102/106 + 9/9 + 59/60 + 30/30 = **200/205 checks、37/41 例**；复跑后两版各只剩 R18（两版都不过，机械原因见上）和 R13（两版都是三次里一次过的波动）。**结论：v2 与 v1 在同一套断言下持平，没有一项是 v1 过、v2 复跑仍不过的。**

时长／成本（Opus 5，每轮）：v1 首跑 wall p50 31.4 s / p90 51.6 s，首字均值 7.1 s，$0.130/轮（45 轮，缓存命中 75/75）；v2 zh wall p50 31.5 s / p90 47.9 s，首字 8.0 s，$0.126/轮（25 轮）；v2 en 首字 5.8 s，$0.089/轮；**v2 id chips $0.53/轮**（6 轮，缓存 4/7 命中——87k tokens 的前缀写一次就是 $0.5 以上，见 §3 第 2 点）。

**R18 为什么两版都不过。** 「没有念过」这一轮的检索是入门功课基线（《入门手册》p23/p36/p29、《175 问》p156…），这些段落里有 21遍／49遍，但 batch 2 的护栏是**成对**绑定：来源里的 21 遍绑的是《往生咒》，不是《解结咒》。所以只要回复写「《解结咒》每天 21 遍」，护栏就判 `number_not_in_sources`，重试一次后 strip 掉这一行（两版四次跑的 `[verbatim-guard]` 日志都是同一条 `text="21遍" reason=number_not_in_sources`）。

- v2 的行为是 C2 要的：四次里每次草稿的功课块都是 大悲咒 3 + 心经 3 + **解结咒 21**（礼佛不在本轮）；被 strip 之后剩下的是「📿 《解结咒》＋祈求词、没有遍数」。
- v1 的行为就是 brief 描述的 R14 漏《解结咒》：两次都只给两部，把解结咒放到「念顺了再加」。
- 所以 R18「各有遍数」这条在现有检索下**任何提示词都过不了**：它需要检索把一段带《解结咒》遍数的原文（例如《佛学问答》第 72 问「解结咒 21、27 或 49 遍」）放进「没有念过」这一轮。这是检索基线／护栏的事，不在 F08 范围，我没动；请架构师定：(a) 给关系类话题的入门轮加一条《解结咒》基线段落（像 batch 1 给失眠加《入门手册》基线那样），或 (b) 把 R18 的断言放宽到「解结咒出现在功课块里」（R14 现在就是这样）。
- 其余断言（礼佛不在本轮、四段结构、祈求词、无拒答尾巴）v2 四次里三次全过；复跑那次 strip 之后多了一句「关于具体的遍数／张数，我目前查不到相关原文」——那是护栏 blanket 尾巴，不是模型写的。

## 5. 合并中发现的新矛盾（列出，不定）

**A. brief 点名之外、但属于同一决定的行——我动了，请确认（都在追溯表里标了「请架构师确认」）：**

1. C1：L1401–1408「小房子的教学时机」整段删。brief 只点 L1404（两周），但 L1405「熟悉经文节奏后，才开始教小房子」、L1406「例外」、L1408「先让他们感受念经的安稳，再引入小房子」是同一句话的展开，只删 L1404 会留下一段自相矛盾的残句。
2. C1：L2278–2289「不熟练时的警告」（「4 部经都要很熟练才能做…先把大悲咒和心经念稳，会往生咒和七佛灭罪真言之后，再学小房子」）和 L2493–2507 模板 E（Tier 0/1 问小房子 → 先 7 遍、加礼佛、学会往生咒七佛后才学）整段删——两处都是 R13 要防的那句话，与 L2124 直接矛盾。
3. C1：L2182–2183「需要小房子 (见 Tier 3)／用户需要先会 4 部经才能做」改为指向入门轮规则第 4 条。
4. C2：L1645 三大支柱「所有修行人必须」里的礼佛条目加了限定「完全没念过／怕难／时间少这一档不在本轮」，否则与分档一（礼佛不在这一轮）正面冲突。
5. C2：L1764「太多了念不完」降档块里的「礼佛 1 遍」删——这一档就是 C2 的「怕难／时间少」。
6. C2：v1 的两个完整示例（L1773–1805、L1870–1884）功课块都是四部完整版，我没改示例正文，只在前面加了一句标明「这是已在修档的写法」。如果架构师希望示例本身改成没念过档，请说一声。
7. C3：L2093「AI 第一件事 = 问」、模板 A「AI 先问」也改成「同一条回复里给暂定功课＋问」——brief 只点了英文 L2677，但写了「全语言统一」。

**B. 没动、请架构师定的矛盾（v1 就有，不在 C1–C8 内）：**

1. **初学者要不要提礼佛**：L77 / L180「初学者先不要念礼佛，从大悲咒心经入手」、L2123 Tier 0「不提礼佛」 vs L636「初学者只教三大支柱（大悲咒 + 心经 + 礼佛大忏悔文 + 日常 21 遍往生咒）」、L1311「三大支柱必须全部提到，不能只提两部」。「介绍」和「开功课」也许是两回事，但提示词没这样说。
2. **英文 Tier 0 的「1-2 weeks」**：L2696–2697「once you're comfortable (1-2 weeks), you can add the Repentance Mantra」 vs 第二十四部分「不设时间预期」（L2572 明确禁止「1-2 周就会好转」）和 C1 删掉的所有「两周」。这是礼佛不是小房子，所以我没按 C1 删。
3. **印尼语回复语言**：L923「用户用马来文/印尼文 → 可简短用他们的语言问候，然后转中文或英文解释」 vs 印尼语包装句「Respond in Bahasa Indonesia」。v2 的 id 拼装沿用了 v1 包装句，L923 原样搬进 tiers。
4. **第十二部分示例**（L1116–1136，core.ts）「我老公外遇，我想离婚」的理想回应也是四部完整功课（含礼佛 1-3 遍），和 C2 分档一不一致；它在「来源与引用」而不是关系类模块，我没加标注。
5. **L2539「先问有没有念过经 (决定 Tier)」**（核心原则汇总 #2）保留了「先问」二字，和 C3「给了再问」的措辞方向不同，意思还算兼容。
6. **危机四步 vs R20**：L816「不要在危机时教完整功课，现在只需要一句菩萨圣号」——家暴属危机四步触发条件，所以「之后才谈功课」在提示词里其实是「之后只给菩萨圣号」。R20 的断言按这个理解写（安全资源在前，其后有念经/菩萨圣号即可），v1、v2 都过。

## 7. 增补（`docs/briefs/2026-09-11-batch-4-addendum.md`，§0 追溯表已批）

### 7.1 改动
- **A6**：第二个示例（v1 L1870–1884）改写成「没念过」档——大悲咒 3 + 心经 3 + 解结咒 21，礼佛不在本轮，祈求词逐字用 v1 L1788/1791/1799 的原句；访客既已说过没念过，示例末尾的「你之前有念过经吗？」去掉（同一段落另两处保留）。第一个示例（L1773–1805）保留为「已在修」档，标注句改为两档各一例。
- **B1**：L77、L180、L1311、L2123 按架构师给的措辞改；L636 前加「标准入门功课（已在修／念顺档）」；架构师那句「提和开是两回事…」放进 tiers 入门轮硬性规则第 5 条。
- **B2**：EN Tier 0「(1-2 weeks)」→「with these two — there's no fixed timeline」。
- **B3**：语言切换三行改为「回复语言＝界面语言；夹别的语言可问候一句再回到界面语言」。
- **B4**：第十二部分示例用户句加「（我平时有念经）」。**B5**：L2539 改「首轮：同一条回复里给暂定功课，并问有没有念过经」。**B6**：家暴条目加「走危机四步：首轮只给安全资源＋一句菩萨圣号；功课等对方安全后、主动再问时再谈」。
- **0.1 (c)**：`assemble.ts` 默认 `overlay`——en/id = 九个中文模块 + v1 的包装句（L2823/L2835「rules below are written in Chinese」那句原样保留，因为现在又是真的）。译文模块留在 `SYSTEM_PROMPT_I18N=translated` 后面，没删、没再跑翻译脚本。体积：v2 zh 49,241 / en 49,553 / id 49,509 tokens（v1 49.6–49.9k）。
- **0.2**：R7 checker 先剔除免责句再断言（照 R16）；CHIP4-zh 在护栏修完、部署后重预热（见 7.3）。
- **R18 护栏**：`verbatim-guard-pairs.ts` `tokensAt` 认枚举——`21、27、49、78 或 108 遍` 每个数各发一个 token（同 span），中文数字同样；范围 `21-49遍` 不动；「念 21 张小房子、7 遍心经」不串。N9 六条断言进 `test-verbatim-guard.ts`：**111/111**。
- 追溯表重生成：v2 中文非空行 1,964，追溯 1,917，新增 47（C1–C8 31 + `addendum-*` 16），未追溯 0；v1 未进 v2 的 73 行全部有依据。五类清单仍 v1 = v2。

### 7.2 v2 + Opus 5 全套（切默认前的那一跑，`suite-v2-opus-full.log`）

| | v1 基线（§4 首跑） | v2 增补后 |
|---|---|---|
| checks | 201/205 | **201/205** |
| 例全绿 | 38/41 | **38/41** |
| 没过的例 | R10、R13、R18 | R10、R14、R18 |
| 每轮 wall p50 / p90 | 31.4 s / 51.6 s | 28.0 s / 49.7 s |
| 首字均值 | 7.1 s | 8.7 s |
| 每轮成本 | $0.130（缓存 75/75） | $0.189（缓存 60/67——前缀刚换、三档各写一次） |
| N1–N9 | 105/105（N9 未加） | **114/114** |
| 18 chips | 18/18 | **18/18** |

那一跑的三个没过（R10 低档波动；R14、R18 的 `解结咒 21遍` 仍被 strip）把 R18 的第二个根子逼了出来：**枚举修好了还是 `number_not_in_sources`——因为 Pinecone 里的《入门手册》p29 是 PDF 抽出来的，写成 `《解结咒》 21 、 27 、 49 、 78\n\n或\n\n108\n\n遍`，护栏按行抽对，数字和「遍」不在同一行，根本抽不出 (解结咒, N遍)。** 加了 `joinBrokenEnumerations()`（数字与紧随其后的 或／、／遍／张／范围线之间只有空白和换行时合回一行；N9 再加三条用真实 p29 块的断言，114/114）。修完：

- R18 × 3（`suite-v2-opus-R18-4/5/6.log`）：**两次 6/6 全绿、护栏 clean**（第 5 次是 Anthropic overloaded 529，没跑成）。
- R14 + R10 复跑：**13/13**，护栏 clean。
- 顺带的收益：p29 的 `《大悲咒》 7\n\n遍`、`《往生咒》 21\n\n或\n\n49\n\n遍` 现在也能成对——以前这些数字只能当裸数字过。
- 小心一点：合行后 `礼佛大忏悔文\n\n3\n\n遍、《解结咒》…` 里的 3 遍会绑到《解结咒》（礼佛大忏悔文 不带书名号时不算主语）。多出一对 (解结咒, 3遍) 可被落地，无害但不精确，记在这里。

护栏修在全套之后，全套没有重跑（修改只会让更多有据的数字通过、不会让通过的变不过）；R18/R14/R10 单独复跑如上。

### 7.3 切默认 + 部署

- `system-prompt.ts` 默认改为 v2（`SYSTEM_PROMPT_VERSION=v1` 可回退，不用改代码）；en/id 走 overlay。
- 合并 `937b030` → Vercel 自动部署（/qa 200）。部署后重预热 CHIP4-zh（孩子不听话）：先把该行 `invalidated_at` 置为 now（不然是缓存命中，0.7 s 就回来了），再走访客路径重生成——**17:21:29 MYT，v2 提示词，护栏 passed_after_retry，655 字，没有 blanket 尾巴，live**（`chip4-rewarm.log`）。其余 17 个 chip 是 14:18–14:27 那批（v1 提示词生成、仍在 24 h 内），夜间 00:30 刷新后全部换成 v2。
- 生产 R17（小房子经文组合）在 batch 3 已按 v1 验过；v2 的 R17 在本地 v2 全套里全绿，没有再到生产上手点。

## 8. 步骤 3：v2 + Sonnet 4.6 A/B（只贴数字，不切模型）

同一套 R1–R21 + 18 chips（入门轮 R12/R13/R14 在 R 套里）+ N1–N9，`REPLY_MODEL=claude-sonnet-4-6 SYSTEM_PROMPT_VERSION=v2`，concurrency 4，分档规则同生产（Sonnet 走顶层 effort 形式，每档一份前缀）。日志 `suite-v2-sonnet46-full.log`。

| | batch 1 Opus 5（v1） | batch 1 Sonnet 4.6（v1） | **v2 Opus 5**（§7.2） | **v2 Sonnet 4.6** |
|---|---|---|---|---|
| checks | — | — | 201/205 | **203/205** |
| 例全绿 | 17/17（final 跑） | 见 batch 1 §6 | 38/41 | **39/41** |
| 没过的例 | — | CHIP6-id 只回分诊问题 | R10、R14、R18（护栏修前） | R4、R19（见下） |
| 每轮 wall p50 / p90 | 27.5 s / 62.3 s | 14.6 s / 33.3 s | 28.0 s / 49.7 s | **18.9 s / 30.2 s** |
| 首字均值 | 13.6 s | 2.8 s | 8.7 s | **2.9 s** |
| 每轮成本 | $0.134 | — | $0.189 | **$0.125**（缓存 51/63） |
| 护栏 strip 次数（41 例） | — | — | 7 | 4 |
| R18（C2 分档） | — | — | 修护栏后 6/6 | **6/6 首跑即过** |
| R14（brief 里 Sonnet 漏《解结咒》的那一例） | — | 失败（brief 的起因） | ✓ | **✓** |
| N1–N9 | 105/105 | — | 114/114 | 114/114（与模型无关） |

**Sonnet 的两个没过：**
- **R4**（解答来信 七百五十）：引了《解答来信疑惑》但没带「（开示于2015年8月17日）」——网站问答来源规则第 1 条的格式项，内容本身有据、护栏 clean。Opus 在 v2 首跑也丢过一次节目日期（R6），性质一样：措辞形态波动，不是编造。
- **R19**（EN 给了再问）：回复写的是「Great Compassion Mantra × 3 / Heart Sutra × 3」，checker 的正则只认「3 times / 3遍」，把「× 3」漏了——**checker 假阴性**，回复本身是 brief 要的样子（同一条回复里两部经＋遍数＋分诊问题）。checker 已改成也认「× N」（这次跑之后改的，没有重跑）。

**读数字时请留意：** 这是 Sonnet 在**干净提示词**下的第一次全套，41 例 39 例全绿、剩下两例一个是格式项一个是 checker；首字 2.9 s、每轮 p50 18.9 s，和 batch 1 的 A/B 一致。**是否切模型由 Ken 看数字决定，我没有动 `REPLY_MODEL` 默认值。**

## 6. 待架构师（增补后的状态）
- §5 的七处删改与六处旧矛盾已在增补里定夺并落实（§7.1）；追溯表已重生成，新增行标 `addendum-*`，请事后过目。
- 默认已切 v2（§7.3）。`REPLY_MODEL` 仍是 Opus 5——Sonnet 4.6 的数字在 §8，切不切由 Ken 定。
- 译文模块（en/id）留在 `SYSTEM_PROMPT_I18N=translated` 后面等校对；不要再跑翻译脚本（加编号回填的做法记在增补 brief 末尾）。
- R19 的 checker 改成也认「× N」之后没有重跑；下次全套自然覆盖。
